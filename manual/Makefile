# StreamLog Manual Generation Makefile
# Uses manual-pipeline for PDF/DOCX generation

PROJECT_NAME := streamlog
MANUAL_DIR := $(shell pwd)
PIPELINE_DIR := $(HOME)/Projects/manual-pipeline

include $(PIPELINE_DIR)/Makefile.include
